# Core Framework
Flask==3.1.2
flask-cors==6.0.2

# Data Processing (pinned for stability)
pandas==2.3.3
numpy==2.3.5
python-dateutil==2.8.2

# Configuration
python-dotenv==1.0

# Testing
pytest==7.0

# Optional: API client for chat features
requests>=2.31.0

# Optional: Advanced caching (enables TTL cache in app)
cachetools>=5.3.0
